#include <stdio.h>

/**
 * main - Entry point
 * Description: Prints a message to the machine using the puts() function
 * Return: Always 0
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
